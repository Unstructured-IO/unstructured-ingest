{
  "//": "Claude Code .mcp.json / Claude Desktop claude_desktop_config.json. Registers both servers in one host: the remote Transform MCP (parse/chunk/embed) and this local STDIO server (land JSON into local storage + search).",
  "mcpServers": {
    "rag-ingest-mcp": {
      "command": "python",
      "args": ["-m", "unstructured_ingest.mcp"],
      "env": {
        "OPENAI_API_KEY": "sk-...",
        "URAG_CHROMA_PATH": "~/.unstructured-rag/chroma",
        "URAG_EMBED_MODEL": "text-embedding-3-small"
      }
    },
    "unstructured-transform": {
      "//": "Remote Transform MCP. Point at your host (SaaS / SND / local platform-api). OAuth is handled by the MCP host on first use.",
      "type": "http",
      "url": "https://<your-transform-mcp-host>/transform-mcp/mcp"
    }
  }
}
