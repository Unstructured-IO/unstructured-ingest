# Unstructured Ingest

Core package support targets Python 3.11 through 3.14. Optional extras that depend on
`pyiceberg` may temporarily lag when upstream dependencies have not published compatible wheels yet.

For details, see the [Unstructured Ingest overview](https://docs.unstructured.io/ingestion/overview) in the Unstructured documentation.
